---
# Per environment values which override defaults in hmpps-launchpad-auth/values.yaml

generic-service:
  ingress:
    host: hmpps-launchpad-auth.hmpps.service.justice.gov.uk

    env:
      APPLICATIONINSIGHTS_CONFIGURATION_FILE: applicationinsights.dev.json
      AZURE_TENANT_ID: AZURE_TENANT_ID
      AZURE_CLIENT_ID: AZURE_CLIENT_ID
      PRISONER_API_USERNAME: HMPPS_AUTH_CLIENT_ID
      PRISONER_API_PASSWORD: HMPPS_AUTH_CLIENT_SECRET
      DB_USERNAME: DB_USERNAME
      DB_PASSWORD: DB_PASSWORD
      DB_ENDPOINT: DB_ENDPOINT

# CloudPlatform AlertManager receiver to route prometheus alerts to slack
# See https://user-guide.cloud-platform.service.justice.gov.uk/documentation/monitoring-an-app/how-to-create-alarms.html#creating-your-own-custom-alerts
generic-prometheus-alerts:
  alertSeverity: digital-prison-service
